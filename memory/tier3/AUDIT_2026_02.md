# AUREM Deep Audit — Customer Experience Reality Check
**Date:** Feb 2026 · **Methodology:** actual code scan + endpoint verification, no assumptions

Legend: ✅ Built + Wired · ⚠️ Built but NOT wired · ❌ Not built · 🟡 Partial

---

## 1. CUSTOMER DASHBOARD
**Status: ⚠️ Built as admin-only — no customer segmentation**

- `AuremDashboard.jsx` is the ONLY dashboard. No role check (`user.role`, `user_type`, `plan_tier`) anywhere in it.
- `ClientManager.jsx` + `CustomerDetail.jsx` exist, but these are ADMIN views of customers, not customer-facing self-service.
- **A customer logging in today sees the same 14-section sidebar as admin** (with Beta toggle to hide "Coming Soon").
- **Customer's own data:** Not surfaced separately — the dashboard shows global metrics, not "your business only" filter.

**Action needed:** Build a `CustomerPortal.jsx` with scoped views, or add role-based sidebar filtering.

---

## 2. WEBSITE
**Status: 🟡 Backend generator works · ❌ `/dashboard/my-website` editor NOT built**

- ✅ `services/website_builder.py::generate_website()` — working
- ✅ `routers/website_builder_router.py::auto_generate_if_missing()` — called from hunt_live, campaign_router
- ✅ Hunt pipeline generates sample site per lead (`aurem.live/sample/{slug}`)
- ❌ **No `/dashboard/my-website` route** — `grep /dashboard/my-website frontend/src` returns ZERO hits
- ❌ **Customer cannot edit** — there's no edit UI, no WYSIWYG, no save endpoint
- ❌ **Nightly Google sync** (refresh customer's hours/reviews/photos from Google Places) — NOT built

**Action needed:** `/dashboard/my-website` editor page + `PATCH /api/website/{slug}` endpoint + nightly `sync_from_google` job.

---

## 3. GOOGLE REVIEWS
**Status: ❌ Not built for customers**

- ❌ **Auto-pull of customer's existing reviews** — no code references `places.getDetails({fields:['reviews']})` or GMB reviews API anywhere
- ❌ **New review request automation** — no "send review link after purchase" flow
- ❌ **Customer dashboard reviews tab** — nothing rendering reviews in dashboard
- ✅ `sentiment_analysis_router.py::analyze_product_reviews` exists but is for YOUR product reviews (meta), not customer's business reviews

**Action needed:** Build `review_automation_service.py` (pull via Google Places API + auto-send review-request WA/SMS post-transaction) + customer dashboard tab.

---

## 4. SOCIAL MEDIA
**Status: ❌ Postiz NOT integrated**

- ❌ **Postiz** — `grep POSTIZ_API_KEY backend` → zero hits. No env var, no integration.
- 🟡 `services/social_media_service.py` exists but is a scaffold — no real Instagram/Facebook/LinkedIn OAuth
- ❌ **Customer dashboard control** — no social tab with per-platform OAuth
- ❌ **Auto-posting** — nothing schedules posts to actual social APIs

**Action needed:** Postiz playbook integration (we have `integration_playbook_expert_v2`) + OAuth flow + scheduled poster.

---

## 5. REPORTS (MONTHLY)
**Status: ⚠️ Admin-only cost reports exist; customer-facing reports NOT built**

- ✅ `tenant_cost_tracker.generate_monthly_report(tenant_id, month)` — but this is internal platform cost tracking, not customer results
- ❌ **Customer monthly report** (businesses contacted, leads won, revenue attributed) — NOT built
- ❌ **Auto-delivery via email/WhatsApp** — no cron scheduling it
- ✅ **Audit PDF** (newly built this session) — `GET /api/compliance/audit-report.pdf` works, but it's compliance data, not a customer-facing monthly business report

**Action needed:** `services/customer_monthly_report.py` + monthly cron + PDF template with their KPIs.

---

## 6. CUSTOMER INBOX
**Status: ✅ Built + Wired (but admin-only UI)**

- ✅ `routers/unified_inbox_router.py` — full CRUD (`GET /{business_id}`, `POST /ingest`, approve/reject/archive/regenerate, bulk archive, stats)
- ✅ Backend dedupes WhatsApp + Email + SMS replies into one collection
- ⚠️ **Customer-facing inbox UI** — the frontend mounts an inbox view but it's scoped through admin auth; no customer self-service view
- ⚠️ **Needs routing fix** — customer should only see their own `business_id` messages (current `/{business_id}` path is not auth-scoped)

---

## 7. ONBOARDING FLOW (POST-STRIPE)
**Status: ✅ Built · ⚠️ Not end-to-end tested with real webhook**

- ✅ `services/aurem_post_payment_onboarding.py::run_post_payment_flow()` — exists, defined at L337
- ✅ Stripe webhook: `aurem_billing_router.py` L223 `stripe_webhook` — wired + signature verified
- ✅ `registry.py` mounts `aurem_billing_router` + `stripe_payment_router`
- ⚠️ **"Google scan auto-trigger"** — `run_post_payment_flow` exists but I didn't verify it actually triggers scout + website + WA welcome. Need end-to-end test.
- ⚠️ **Welcome WhatsApp** — path exists but never confirmed sent to a real paying customer

**Action needed:** Fire a test Stripe payment end-to-end in staging, capture the full chain.

---

## 8. PLAN LIMITS
**Status: 🟡 Service exists, enforcement unclear**

- ✅ `services/usage_metering_service.py` exists
- ✅ `middleware/tier_metering*.py` exists (mounted as middleware per `server.py` startup logs)
- ✅ `routers/subscription_router.py` + `integration_api.py` handle plan data
- ⚠️ **Starter/Growth/Enterprise actual limit values** — defined somewhere but not verified they gate real actions
- ⚠️ **Upgrade/downgrade UI flow** — subscription router has endpoints, frontend UI not verified

**Action needed:** Print current limits, try to exceed Starter quota, confirm 402 response.

---

## 9. REFERRAL SYSTEM
**Status: ✅ Backend agent built · ⚠️ No customer-facing UI**

- ✅ `routers/referral_router.py` — exists (pre-existing)
- ✅ `services/agents/referral_ora.py` — brand new this session, runs nightly
- ❌ **Customer "Refer a friend" UI** — no form where a customer submits a referral
- ❌ **Reward system** — no credit/discount tracking tied to successful referrals (`referrer_notified` flag exists; actual reward application missing)

**Action needed:** `/dashboard/refer` page + `POST /api/referrals/submit` + reward credit to Stripe customer.

---

## 10. CASL + LEGAL
**Status: 🟡 CRITICAL COMPLIANCE GAP**

- ✅ `services/casl_compliance.py` — built + unit-tested
- ✅ `/api/unsubscribe` — public landing page + POST both work
- ✅ Audit trail collections exist (`audit_trail_daily`, `consent_records`, `financial_log`, `learning_log`, `a2a_events`)
- ✅ PDF Audit Export — works
- ✅ HST tracking — computed in day_close (13% Ontario rate, `financial` dict with `hst_included_cad`)
- 🔴 **THE GAP:** **227 existing outbound-send call-sites** in main code send WITHOUT the CASL wrapper. Only the 4 new agent files use `wrap_email_html` / `wrap_sms` / `wrap_whatsapp`. Every Resend email, every Twilio SMS, every WHAPI send outside the agents is CASL-non-compliant.

**Action needed (P0):** Patch the 3 core send helpers (`services/whatsapp_service.py`, main email dispatch, Twilio SMS dispatch) to wrap with CASL before send. One-time change, one-line per helper.

---

## 11. 4 AGENT SYSTEM
**Status: ✅ ALL built + wired (this session)**

- ✅ 🔍 Hunter ORA — `services/agents/hunter_ora.py` — daily rotation, ramp-aware
- ✅ 📣 Follow-up ORA — `services/agents/followup_ora.py` — Day 3/7/14/21/30/60 drip
- ✅ 💰 Closer ORA — `services/agents/closer_ora.py` — competitor/case-study/call/50%-off sequence
- ✅ 🤝 Referral ORA — `services/agents/referral_ora.py` — warm intro + referrer notify
- ✅ A2A bus — `services/a2a_bus.py` — pub/sub + persisted to `a2a_events` + SSE broadcast
- ✅ Auto-Hunt Scheduler — `routers/agents_router.py` + scheduled cron every 30min 06:00–20:00
- ✅ Admin Command Center UI — `frontend/src/platform/AgentCommandCenter.jsx` — 4 cards, ramp selector, dry-run sliders, PDF button
- ✅ Ramp modes (🐢 Safe / 🚀 Aggressive)

---

## 12. NIGHTLY CYCLE
**Status: ✅ ALL built + scheduled**

- ✅ 23:00 Day Close — retry failed, DNC sync, HST close, audit seal
- ✅ 00:00 Next Day Prep — queue build, API preflight
- ✅ 02:00 Auto-Learn — 7-day industry/channel analysis → `learning_log`
- ✅ 19:00 Evening Brief — WhatsApp with ramp mode line
- ✅ 07:00 Morning Brief — already existed, now with ramp mode line too (added this session)

---

## 13. TECHNICAL ISSUES
- ✅ **Redis connections** — CONSOLIDATED this session from 8 independent pools (~85 conns) to 1 shared pool (25 max). Previous 90% warning → now ~5% on local stress test. 200 parallel req verified.
- 🟡 **Platform speed / login time** — not measured. Login API returns fast; frontend first-load depends on the 14-section sidebar rendering. Would need a Lighthouse run.
- ✅ **ORA PWA** — `ora_pwa_router.py` mounted, `manifest.json` present. Not deeply tested though.
- ✅ **Sample website generation** — `generate_website` + `auto_generate_if_missing` working, called from hunt pipeline

---

# Priority Fix Order (my honest take)

## 🔴 P0 — Deploy-blocker / Legal risk
1. **CASL wrapper on all 227 outbound sends** — one-line fix in 3 helper files. Without this, every non-agent message is CASL-non-compliant.
2. **Post-Stripe onboarding end-to-end test** — user said "after Stripe payment — exactly what happens?" — nobody has verified the full chain fires.

## 🟠 P1 — Customer deliverability gap
3. **`/dashboard/my-website` editor** — customers can't edit their site
4. **Google Reviews auto-pull + review-request automation** — table stakes for a business-growth product
5. **Role-based dashboard** — customer vs admin segmentation (currently both see the same thing)
6. **Customer Monthly Report** (PDF + auto-delivery)

## 🟡 P2 — Revenue levers
7. **Postiz integration** — social auto-posting
8. **Referral UI** + reward application
9. **Plan limit enforcement verification** (try to exceed Starter quota, confirm 402)
